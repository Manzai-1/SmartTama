Start blockchain:

```zsh
npx hardhat node
```

Compile contract:

```zsh
npx hardhat compile
```

Deploy contract:

```zsh
npx hardhat ignition deploy ./ignition/modules/Tamagochi.ts --network localhost
```
